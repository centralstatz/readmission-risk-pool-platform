# The main internal namespace exposes only software-owned primitives with an
# accepted owner. Resource access requires an explicit root; Stage 3 separately
# owns installed operation and version-selection interfaces.
NULL
