# This source file establishes the main internal package and dependency owner.
# Resource access, canonical/runtime/project contracts, explicit project
# loading, minimal project initialization, structured project diagnosis, and
# selected producer execution, and semantic provider validation are implemented
# at package-owned boundaries.
NULL
